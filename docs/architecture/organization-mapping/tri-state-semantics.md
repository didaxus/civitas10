# Tri-state semantics

Mapping evaluation reports `matched`, `not_matched`, or `ambiguous`. Only explicit backend approval can move matched evidence into organization-model configuration. Ambiguous, stale, incompatible, or unknown states fail closed and require reconciliation.
