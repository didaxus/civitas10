# Publication and rollback

Publication must target an exact immutable model version and is deferred beyond Turn 1. Rollback creates a new draft from an immutable historical version; it does not mutate history.
