// Public DTOs follow contracts/openapi/modules/planning.yaml. Persistence and
// runtime aliases are accepted only at this boundary and never leak publicly.
function planDto(input){ const content=input.content && typeof input.content==='object' ? input.content : {}; return Object.freeze({ id:String(input.id ?? input.planId), title:String(input.title ?? input.name ?? ''), planType:input.planType||'operational', description:input.description ?? content.description ?? null, status:input.status||input.state||'draft', version:String(input.version||input.etag||'1'), updatedAt:input.updatedAt||new Date(0).toISOString() }); }
function pageDto(input){ const source=input.data||input.items||[]; const page=input.page||{}; return Object.freeze({ data:source.map(planDto), page:Object.freeze({ nextCursor:page.nextCursor||null, hasMore:page.hasMore ?? Boolean(page.nextCursor) }), links:Object.freeze(input.links||{}), meta:Object.freeze(input.meta||{}) }); }
function profileDto(input){ const configuration=input.configuration && typeof input.configuration==='object' ? input.configuration : {}; const preferences=input.preferences ?? configuration.preferences ?? {}; return Object.freeze({ organizationId:String(input.organizationId), planningMode:input.planningMode ?? configuration.planningMode ?? 'standard', preferences:Object.freeze({ ...preferences, fiscalYearStart:preferences.fiscalYearStart||'01-01' }), version:String(input.version||'1'), updatedAt:input.updatedAt||new Date(0).toISOString() }); }
function envelopeDto(data, meta={}){ return Object.freeze({ data, meta:Object.freeze(meta) }); }
module.exports = { planDto, pageDto, profileDto, envelopeDto };
