"use strict";
const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");
test("governance production composition cannot use in-memory persistence",()=>{const files=["index.js","governance/compositionRoot.js","services/governanceRolesReadModel.js","services/governanceStructureReadModel.js","services/governanceOperationsReadModel.js"];for(const file of files){const source=fs.readFileSync(path.join(__dirname,"..",file),"utf8");assert.doesNotMatch(source,/createInMemory[A-Za-z]+/g,`${file} composes an in-memory fake`);assert.doesNotMatch(source,/(?:audit|outbox)\w*\s*=\s*\[\]/gi,`${file} stores audit/outbox in an array`)}const root=fs.readFileSync(path.join(__dirname,"..","governance/compositionRoot.js"),"utf8");assert.doesNotMatch(root,/new Map\s*\(/,"composition root stores state in a Map")});
